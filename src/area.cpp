#include "area.h"
using namespace std;

Area::Area(vector<int> riverXCorners, vector<int> riverYCorners) : riverXCorners(riverXCorners), riverYCorners(riverYCorners){
	
}

bool Area::isInsideRiver(int x, int y){
	
}
